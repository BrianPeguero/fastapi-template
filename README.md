# fastapi-template

template folder structure for production level api applications in fastapi

\*set up the env file based on the dot_env_tamplate file and the database like in the models

to start the application type this into the terminal:
`python -m venv venv`
`venv\Scripts\activate`
`pip install -r requirements.txt`
`py app/main.py`
